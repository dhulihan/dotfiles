#!/bin/bash
# gobytes "{0x30, 0x43, 0x3b, 0xac, 0xf9, 0xb1, 0x41, 0x33, 0xa0, 0xe1, 0xd3, 0xaf, 0x68, 0x16, 0x6e, 0x1d}" # floop

while read line
do
  echo "$line"
done < "${1:-/dev/stdin}"

TESTDATA=$(echo -n $1 | tr '{|}' '' | tr ', ' ' ')
for c in $TESTDATA; do
    echo $c | xxd -r
done
